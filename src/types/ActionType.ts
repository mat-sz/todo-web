export enum ActionType {
    // Application state
    SET_ERROR,
    DISMISS_ERROR,
    SET_LOADING,
    SET_TITLE,

    // Authentication state
    SET_LOGGED_IN,
    SET_USER,

    // Settings
    TOGGLE_DARK_THEME,
    SET_TOKEN,
};